@page "/requirements"
@inject IMarkdownDocumentProvider Documents

<PageTitle>Requirements · PowerPosition</PageTitle>

@* The document carries its own top-level heading, so the page adds none. *@
<article>@Documents.Render(MarkdownDocument.Requirements)</article>
